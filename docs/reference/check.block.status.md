# check.block.status

Checks if a block is defined.

## Usage

``` r
check.block.status(block)
```

## Arguments

- block:

  A dlm_block object.

## Value

A character ("defined" or "undefined") indicating if all parameters in
the block are defined.
